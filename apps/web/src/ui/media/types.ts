/** Размер медиа-компонента под место использования. */
export type MediaSize = 'queue' | 'submit';

/** Тип медиа в превью/плеере. */
export type MediaKind = 'image' | 'video' | 'youtube' | 'audio';
